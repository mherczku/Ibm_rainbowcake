package hu.hm.ibm_rainbowcake.ui.detail

import javax.inject.Inject

class DetailPresenter @Inject constructor(
) {
    // Api does not provide GET(itemID: ID) function -> Presenter hasn't got work to do
}